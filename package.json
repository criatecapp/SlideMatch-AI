{
  "name": "slidematch-ai",
  "private": true,
  "workspaces": ["api", "frontend"],
  "scripts": {
    "test": "npm run test --workspace=api",
    "typecheck": "npm run typecheck --workspace=api && npm run typecheck --workspace=frontend"
  }
}
